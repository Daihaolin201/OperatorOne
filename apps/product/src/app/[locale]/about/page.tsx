export default function AboutPage() {
  return (
    <main className="min-h-screen bg-product-50 p-8">
      <div className="mx-auto max-w-2xl">
        <h1 className="text-3xl font-bold text-product-900">About OperatorOne</h1>
        <p className="mt-4 text-product-500">
          OperatorOne is a multi-agent execution platform that automates the full startup loop:
          idea discovery → demand generation → outreach → feedback-driven iteration.
        </p>
      </div>
    </main>
  );
}
